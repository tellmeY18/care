name: Print Environment Variables

on:
  push:
    branches:
      - "*"
  workflow_dispatch:
jobs:
  print_env:
    runs-on: ubuntu-latest
    environment: plugs
    steps:
      - name: Print Environment Variables
        run: |
          printenv
          echo "ADDITIONAL_PLUGS=${ADDITIONAL_PLUGS}"
